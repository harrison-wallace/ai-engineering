---
name: session-prune
description: Propose archive/index tidy of docs/plans and docs/phases so /session-start stays cheap. Confirm each file. Use when plans are noisy or the user runs /session-prune.
user-invocable: true
---

# session-prune

Read and follow `session-handshake` (sibling skill, or
`~/.claude/skills/session-handshake/SKILL.md`). Run **Prune** only.

Propose. Wait. Apply only what the user confirms. Do not delete, merge phase
docs, rewrite a master plan, or tick checkboxes.
